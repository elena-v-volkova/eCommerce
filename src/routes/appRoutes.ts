export enum AppRoute {
  profile = '/profile',
  login = '/login',
  register = '/register',
  catalog = '/catalog',
  home = '/',
  about = 'about',
  product = 'products/:key',
  cart = '/cart',
  notFound = '*',
}
